

// Exporting file => Exporting a Single function 
// used method => first method [one accululated export] 


// Define the main function :
const sayHello = (message) => {
    console.log(message) ;
}

// Exporting the single defined function using the [accululated export] :
export default sayHello ; 
